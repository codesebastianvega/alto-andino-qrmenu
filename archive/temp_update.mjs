import { createClient } from '@supabase/supabase-js'; import fs from 'fs'; const env = fs.readFileSync('.env', 'utf8').split('\n').reduce((acc, line) => { const parts = line.split('='); if(parts.length >= 2) acc[parts[0].trim()] = parts.slice(1).join('=').trim(); return acc; }, {}); const supabase = createClient(env.VITE_SUPABASE_URL, env.VITE_SUPABASE_ANON_KEY); async function run() { const {data, error} = await supabase.from('brands').update({ city: 'Zipaquirá', description: 'Restaurante de comida artesanal en las nubes, ofreciendo experiencias gastronómicas únicas.' }).eq('slug', 'alto-andino'); console.log('Done', error); } run();